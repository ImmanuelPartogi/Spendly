include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Treat these as errors — they indicate real bugs.
    avoid_print: error
    avoid_dynamic_calls: error
    empty_catches: error
    empty_statements: error
    avoid_returning_null_for_future: error
    unawaited_futures: warning
    # Deprecations should be surfaced prominently.
    deprecated_member_use: warning
    # Disable overly strict inference warnings (style preferences, not bugs).
    inference_failure_on_instance_creation: false
    inference_failure_on_function_invocation: false
    inference_failure_on_untyped_parameter: false
    inference_failure_on_collection_literal: false

linter:
  rules:
    # ── Error-prone patterns ──────────────────────────────────────────────────
    avoid_print: true
    avoid_dynamic_calls: true
    avoid_returning_null_for_future: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    empty_catches: true
    empty_statements: true
    hash_and_equals: true
    collection_methods_unrelated_type: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    prefer_relative_imports: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true
    use_build_context_synchronously: true
    valid_regexps: true

    # ── Style & consistency ───────────────────────────────────────────────────
    always_declare_return_types: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    prefer_single_quotes: true
    prefer_interpolation_to_compose_strings: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unawaited_futures: true
    use_decorated_box: true
    use_full_hex_values_for_flutter_colors: true
    use_named_constants: true
    use_super_parameters: true

    # ── Documentation ─────────────────────────────────────────────────────────
    public_member_api_docs: false
    lines_longer_than_80_chars: false